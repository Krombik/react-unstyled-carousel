export enum Folder {
  COMPONENTS = 'components',
  HOOKS = 'hooks',
  MODULES = 'modules',
  TIMING_FUNCTIONS = 'timingFunctions',
  CHUNKS = '_chunks',
}

export const FILES_TO_COPY = ['LICENSE', 'README.md'];
